{
  "name": "golf-diary-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web-app"
  ],
  "scripts": {
    "start": "npm start -w web-app",
    "build": "npm run build -w web-app",
    "test": "npm test -w web-app"
  }
}
