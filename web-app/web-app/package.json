{
  "name": "golf-diary-web-forwarder",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd .. && npm install && npm run build && mkdir -p web-app/build && cp -r build/* web-app/build/"
  }
}
